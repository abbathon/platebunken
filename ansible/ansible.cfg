[defaults]
inventory = inventory.yml
roles_path = roles
host_key_checking = True
# Default callback on purpose: the `yaml` one lives in community.general, and this playbook
# deliberately requires no Galaxy collections.
stdout_callback = default
interpreter_python = auto_silent
retry_files_enabled = False

[ssh_connection]
pipelining = True
